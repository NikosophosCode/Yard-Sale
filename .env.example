# API Configuration
# Set to 'true' to use mock data instead of json-server (recommended for production)
VITE_USE_MOCK=false

# API URL (only used when VITE_USE_MOCK=false)
VITE_API_URL=http://localhost:3001
